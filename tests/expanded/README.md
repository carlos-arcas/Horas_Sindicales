# Expanded test suite

Esta carpeta documenta la ampliación de cobertura con foco en reglas de negocio y persistencia.

- `tests/domain/`: validaciones y cálculo de tiempos.
- `tests/application/`: resolución de conflictos y normalización de sync.
- `tests/infrastructure/`: ramas de repositorios SQLite y validación de SQL.
